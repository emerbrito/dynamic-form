export const enum ButtonType {
    Basic = 'basic',
    Raised = 'raised',
    Stroked = 'stroked',
    Flat = 'flat',
    Fab = 'fab'
}

export const enum Color {
    Primary = 'primary',
    Accent = 'accent',
    Warn = 'warn'
}

export const enum ContentAlignment {
    Left = 'start',
    Right = 'end'
}

export const enum Appearance {
    Standard = 'standard',
    Fill = 'fill',
    Outline = 'outline'
}

export const enum ControlType {    
    Datepicker = 'datepicker',
    Dropdown = 'dropdown',
    MultiSelect = 'multiselect',
    NumericInput = 'numeric',
    RadioGroup = 'radiogroup',    
    Slider = 'slider',    
    TextArea = 'textarea',
    TextBlock = 'textblock',
    TextInput = 'input',    
    Timepicker = 'timepicker',
    ToogleButton = 'tooglebutton'
}

export const enum DatePickerViewMode {
    Month = 'month',
    Year = 'year',
    MultiYear = 'multi-year'
}

export const enum LabelDisplayMode {
    Detached = 'detached',
    FloatAuto = 'auto',
    FloatAlways = 'always',
    FloatNever = 'never'
}

export const enum StepperLabelPosition {
    End = 'end',
    Bottom = 'bottom'
}

export const enum ToggleMode {
    SlideToggle = 'slidetoggle',
    CheckBox = 'checkbox'
}